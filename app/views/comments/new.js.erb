$("#comment").html("<%= escape_javascript(render partial: 'new' ) %>")
$(".modal-title").text("Add New Comment")
$(".submit").attr("value","Add New Comment")
tinymce.remove();
tinymce.init({selector:'textarea'});
$("#comment").modal('show')
