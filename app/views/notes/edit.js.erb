$("#note_comment").html("<%= escape_javascript(render partial: 'edit' ) %>");
$(".modal-title1").text("Edit Note");
$(".submit").attr("value","Edit Note");
tinymce.remove();
tinymce.init({selector:'textarea'});
