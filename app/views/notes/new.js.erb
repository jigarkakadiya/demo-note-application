$("#note_comment").html("<%= escape_javascript(render 'new') %>");
$(".modal-title1").text("Add New Note");
$(".submit").attr("value","Add Note");
tinymce.remove();
tinymce.init({selector:'textarea'});
